/**
 * RSC-safe schema-only entry — no renderer, no client boundary.
 */
export { WeavePageConfigSchema, WeaveSectionSchema } from './schemas/page-config';
export type { WeavePageConfig, WeaveSection } from './schemas/page-config';
