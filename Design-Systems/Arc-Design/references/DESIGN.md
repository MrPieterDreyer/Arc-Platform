# Design tokens (extended)

The canonical token list lives in:

- **`../DESIGN.md`** — human-readable summary
- **`../tokens/arc-tokens.css`** — CSS variables to import in apps

Do not duplicate token definitions here. Update `arc-tokens.css` and `DESIGN.md` together when tokens change.
