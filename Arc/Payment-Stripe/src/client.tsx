'use client';

/**
 * @arc-platform/payment-stripe/client — client-only barrel
 *
 * Import via `@arc-platform/payment-stripe/client` from Client Components only.
 * Stripe Elements and React hooks live here (Plan 06).
 *
 * Nothing exported yet — Plan 06 adds the StripeProvider and useStripeCheckout hook.
 */

export {};
